сделаем на фаст апи и скл лайт и реакт само собой прилодение.                            
три вертикальные панели раздеоенные салриттером.
слева иерарзхия - на верзнем уровне добавлем модель
внутри сразу папки рисуются 
 листы
   наименование, также модно добавыить любые аналатики (ниже расписано), и установить из порядок
 
 аналитики

   аналитики - это иерархическеи справочники, если добавилии и встали на нее в тривтбю - в центре появлояются настройки
   иконка (()выбар из MUI реально  видно иконки и отоброаждаетя при выборре на форме) наименование (транслит из наименования автоматом, модно менять)
   признак - периоды, если отметить (дефолт - нет), то список полей  (ниже) прелдопределен -  наименование начало окончание.
   кроме того можно выбирать список типов периодов (месяц, квартал год) - любве из жтихю
   начало и окочкание (даты).
   если выбрать период - все заполнится автоматически если типов периодов > 1 - c иерархией
      2026
         1-й квартал
            январь
            ...
         2-й квартал
   следующзая секция      
   список полей (наименование тип - число строка % деньги дата)

   следующая секция - там тригрид - на котором можно джоабвлять новые записи. на тулбаре тригрида загрузка из екселя и обратно
   логика слеюущая - иерразхию отображает отступами (ну напрмер 4), это мы выгружаем с 4, щзагружаем любые отстсупыф понимая что жто иерархия. 



---
в конечном итоге моджно завести любое количетсво аналитик и привязать их к листам, тодже в олюбом количестве.
на листе кнопка (просмотр), там появлятся форма поверх, на котором следующая логика реализована
првая аналитика из спика - идепредставляет столбюцы. если есть иерарзхия - риуем влодженные колонки, типа наверз год ниже кварталы, третий уровень столбцов - месяца.
все остальыне идут по порядку. дадее мы римуем иерархию верхней аналитики (второй по порядку), под ней соледлующая - тодже иерарзия итд
сть кнопка с шестереной гед мы можем поменять порядок и тогда все перерисуется)
есть вохмодность любые аналитики зафиксировать (выбрать значение, это будет отрображдаться на специальной панели и тогда ее не показычвеам.)

группирубщие аналитики -м там ввод запрещен, туда числовые значения скммируются по оефодту введенные на нижнем уцровне (на пересечении конкретных значенимй терминальных аналитик)

ух много всего, но на самом деле я думаб мысль ты уловил - это что-то типа анаплана маленького. потом будет докручивать.
дизайн весь в NUI стиле полегче и попроще, можешь как пример для подразжания посмотреть формы отсюда.
/Users/mac/PROJECTS/custom-forms